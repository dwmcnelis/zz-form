{{#unless template}}
    {{#if label}}
        {{#if yieldInLabel}}
            {{#if labelWrapperClass}}
                <div {{bind-attr class=labelWrapperClass}}>
                    {{partial 'components/zz-formgroup/zz-control-within-label'}}
                </div>
            {{else}}
                {{partial 'components/zz-formgroup/zz-control-within-label'}}
            {{/if}}
        {{else}}
            {{#if labelWrapperClass}}
                <div {{bind-attr class=labelWrapperClass}}>
                    {{zz-form-label text=label viewName=labelViewName}}
                    {{partial 'components/zz-formgroup/zz-form-group-control'}}
                </div>
            {{else}}
                {{zz-form-label text=label viewName=labelViewName}}
                {{partial 'components/zz-formgroup/zz-form-group-control'}}
            {{/if}}
        {{/if}}
    {{else}}
        {{partial 'components/zz-formgroup/zz-form-group-control'}}
    {{/if}}

    {{#if show_icons}}
        <span class="form-control-feedback"><i {{bind-attr class=v_icon}}></i></span>
    {{/if}}

    {{!Currently no errors when layout is inline}}
    {{#unless form.isInline}}
        {{#if canShowErrors}}
            {{zz-form-group-help text=help viewName=helpViewName}}
        {{/if}}
    {{/unless}}
{{else}}
    {{yield}}
{{/unless}}